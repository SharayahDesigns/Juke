// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1c76c0', 
    },
    secondary: {
      main: '#ffa500', 
    },
  },
});

export default theme;
